# Pro Downloader — التعليمات

## الفكرة باختصار
البرنامج بيتكون من الملفات دي، لازم تكون كلها في نفس المجلد:

- `main.py`               ← نقطة تشغيل البرنامج
- `downloader_core.py`     ← منطق التحميل الفعلي (بيستخدم مكتبة yt-dlp)
- `web` (مجلد)            ← شكل الواجهة (HTML/CSS/JS) — **لازم يفضل اسمه "web"**
- `build_program.bat`      ← ملف البناء
- `ProDownloader.ico` و `ProDownloader.png` ← أيقونة البرنامج

مش ممكن تحويل ملف بايثون إلى exe إلا لما يتبني على نفس نوع النظام
اللي هيشتغل عليه، فلازم تبني الملف مرة واحدة على جهاز ويندوز (جهازك كفاية).
بعد كده الملف الناتج تقدر تبعته لأي حد، وهيفتح عندهم زي أي برنامج عادي.

## الخطوات

1. تأكد إن عندك بايثون **3.12** بالتحديد مثبت (مش 3.14 - فيه مكتبة
   البرنامج محتاجها لسه ما بتدعمهاش). حمّله من:
   https://www.python.org/downloads/release/python-3120/
   وفعّل "Add python.exe to PATH" أثناء التثبيت.

2. حط كل الملفات المذكورة فوق في نفس المجلد (مجلد واحد، اسمه بالإنجليزي).

3. دبل كليك على `build_program.bat`
   (لو ظهرت رسالة أمان من ويندوز، دوس "More info" ثم "Run anyway")

4. استنى — هيحمّل المكتبات ويبني البرنامج (يستغرق من 5 لـ 15 دقيقة).

5. هتلاقي على **سطح المكتب** ملف اسمه `ProDownloader.exe`.

## الاستخدام
- الصفحة الرئيسية: الصق رابط الفيديو (من يوتيوب/تيك توك/فيسبوك/إنستجرام)،
  اختار الجودة، ودوس "تحميل الآن".
- الجودة المطلوبة سقف بس — البرنامج بيجيب أعلى جودة متاحة فعليًا للفيديو
  ده حتى الحد اللي اخترته. مش كل فيديو متاح بـ8K أو حتى 4K (خصوصًا في
  تيك توك وإنستجرام وفيسبوك، غالبًا أعلى جودة متاحة أقل من كده بكتير).
- السجل: بيتفظ فعليًا على جهازك (مش بيتمسح لما تقفل البرنامج)، وفيه زرار
  "إعادة المحاولة" لأي عملية فشلت.
- الإعدادات: تقدر تغيّر مكان حفظ الفيديوهات، اللغة، والوضع الداكن/الفاتح.

## ملاحظات مهمة
- **WebView2**: البرنامج بيعرض واجهته عن طريق مكوّن WebView2 بتاع
  مايكروسوفت (موجود افتراضيًا في ويندوز 10/11 الحديث). لو نادرًا حصل
  والنافذة ما فتحتش، حمّله من:
  https://developer.microsoft.com/microsoft-edge/webview2/
- **التحديثات**: منصات زي يوتيوب وتيك توك بتغيّر تقنياتها بين فترة
  وفترة، ومكتبة yt-dlp بتتحدث باستمرار عشان تفضل شغالة. لو بعد فترة
  التحميل بدأ يفشل بشكل متكرر، الحل الأرجح إنك تعيد بناء البرنامج تاني
  (امسح `build` و`dist` وشغّل `build_program.bat` من جديد) عشان يجيب
  أحدث نسخة من yt-dlp.
- **محتوى خاص/محمي**: فيديوهات إنستجرام وفيسبوك الخاصة أو اللي محتاجة
  تسجيل دخول ممكن ما تنزلش من غير خطوات إضافية (البرنامج ده مصمم
  للمحتوى العام المتاح لأي حد).
- لو حبيت تبني نسخة تانية بعد تعديل الكود، امسح مجلدي `build` و`dist`
  وملف `ProDownloader.spec` وشغّل `build_program.bat` تاني.

## تجربة سريعة من غير تحويل لـ exe (اختياري)
```
pip install yt-dlp pyperclip pywebview pythonnet
python main.py
```
